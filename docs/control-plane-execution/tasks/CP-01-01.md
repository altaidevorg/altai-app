# Task Packet: CP-01-01 — Core ID, Revision, Actor, Error, and Event Contracts

```yaml
task_id: CP-01-01
title: Create altai-control-protocol Rust crate and control-contract TS package with core ID, revision, actor, error, and event types
risk_tier: A
parent_module: CP-01
status: accepted
depends_on: [CP-00-02]
objective: |
  Create the shared domain contracts that all subsequent CP modules depend on:
  typed IDs for every canonical concept, Revision for optimistic concurrency,
  Actor identity for mutations, typed errors, and ActivityEvent envelopes.
  Both Rust and TypeScript must serialize to the same golden fixtures.
non_goals:
  - Creating the altai-control-plane crate (CP-02)
  - Creating SQLite migrations or repositories
  - Implementing domain services or commands
  - Creating UI components
  - Modifying existing agent-protocol or host-contract packages
allowed_files:
  - src-tauri/crates/altai-control-protocol/ (new crate)
  - src-tauri/Cargo.toml (add workspace member only)
  - packages/control-contract/ (new package)
  - pnpm-workspace.yaml (add package only if needed)
  - shared/control-protocol/v1/ (new fixtures)
  - docs/control-plane-execution/tasks/CP-01-01.md (this packet)
  - docs/control-plane-execution/CURRENT_STATE.md (acceptance record)
read_first:
  - docs/control-plane-execution/CONTEXT.md (full)
  - docs/PAPERCLIP_STYLE_CONTROL_PLANE_ENGINEERING_PLAN.md Section 3.3 (one identity per concept)
  - docs/PAPERCLIP_STYLE_CONTROL_PLANE_ENGINEERING_PLAN.md Section 4 (domain vocabulary)
  - docs/PAPERCLIP_STYLE_CONTROL_PLANE_ENGINEERING_PLAN.md Section 5 (state model)
  - docs/PAPERCLIP_STYLE_CONTROL_PLANE_ENGINEERING_PLAN.md CP-01 (full)
  - packages/agent-protocol/src/control-fixture-cal.ts (existing WorkItemId pattern)
  - src-tauri/crates/altai-protocol/Cargo.toml (crate structure pattern)
invariants:
  - Every canonical ID is a distinct typed struct, never a bare string.
  - IDs serialize as {"type":"<kind>","value":"<prefix>..."} matching the existing work_item_id fixture.
  - Revision is a monotonically increasing u64 per aggregate.
  - Actor identifies who performed a mutation; it is never inferred from a session ID.
  - All errors are typed enums, never string matching.
  - Rust and TypeScript produce byte-identical JSON for the same fixture.
  - No existing package is modified (only new files and workspace registration).
commands: []
events:
  - control_event (append-only per-aggregate event with sequence)
migrations: []
tests_required:
  - "cargo test --manifest-path src-tauri/Cargo.toml -p altai-control-protocol"
  - "pnpm --filter @altai/control-contract test"
  - "cargo check --manifest-path src-tauri/Cargo.toml"
  - "pnpm exec tsc --noEmit"
acceptance:
  - "altai-control-protocol Rust crate exists and compiles"
  - "packages/control-contract TS package exists and passes type check"
  - "All 16 canonical ID types parse and serialize correctly"
  - "Invalid IDs fail with typed errors on both sides"
  - "Revision, Actor, ControlError, ActivityEvent types exist on both sides"
  - "Rust and TypeScript validate the same golden fixtures byte-identically"
  - "No existing package or crate is modified"
stop_conditions:
  - "A required ID type cannot be represented in both Rust and TypeScript without diverging"
  - "The workspace structure prevents adding a new crate without breaking existing builds"
```

## Current relevant code facts

### Existing ID pattern (CAL-02)

`packages/agent-protocol/src/control-fixture-cal.ts` defines `WorkItemId` as
`{ type: "work_item_id", value: "wi_..." }`. The Rust side in
`src-tauri/crates/altai-protocol/tests/control_fixture_cal.rs` mirrors this.
The shared fixture is at `shared/control-protocol/v1/fixtures/work-item-id.json`.

### Canonical IDs (parent plan §3.3)

```
organization_id, goal_id, project_id, workspace_id,
agent_instance_id, agent_profile_id + agent_profile_revision_id,
work_item_id, attempt_id, run_id, session_id,
routine_id + routine_revision_id + routine_run_id,
approval_id, external_object_id
```

### Existing crate structure

`src-tauri/crates/altai-protocol/` is a standalone crate with `serde` and
`serde_json` only. The new `altai-control-protocol` crate follows the same
pattern.

## Expected diff shape

1. **New Rust crate** `src-tauri/crates/altai-control-protocol/`:
   - `Cargo.toml` — serde + serde_json
   - `src/lib.rs` — re-exports
   - `src/id.rs` — 16 typed ID structs + parse/serialize
   - `src/revision.rs` — Revision type
   - `src/actor.rs` — Actor enum
   - `src/error.rs` — ControlError enum
   - `src/event.rs` — ActivityEvent + ControlEvent
   - `tests/` — round-trip and rejection tests

2. **New TS package** `packages/control-contract/`:
   - `package.json` — @altai/control-contract
   - `tsconfig.json`
   - `src/index.ts`
   - `src/ids.ts` — 16 typed IDs + parse/serialize
   - `src/revision.ts`
   - `src/actor.ts`
   - `src/error.ts`
   - `src/event.ts`
   - `src/__tests__/ids.test.ts`
   - `src/__tests__/fixtures.test.ts`

3. **Golden fixtures** `shared/control-protocol/v1/fixtures/`:
   - One JSON file per ID type (16 files)
   - `actor.json`, `revision.json`, `activity-event.json`

4. **Workspace registration**:
   - `src-tauri/Cargo.toml` — add `crates/altai-control-protocol` to members
   - `pnpm-workspace.yaml` — add `packages/control-contract` if needed

## Required negative tests

- Empty ID value rejected with typed error
- Wrong type field rejected with typed error
- Missing prefix rejected with typed error
- Non-object input rejected with typed error
- Oversized payload rejected

## Compatibility and deletion effects

- None — new crate and package only, no existing code modified.

## Output/handoff format

Return the standard handoff report with exact test outcomes.
