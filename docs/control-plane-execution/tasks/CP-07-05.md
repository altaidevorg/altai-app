# Task Packet: CP-07-05 — Dispatch Eligibility Engine

```yaml
task_id: CP-07-05
title: Require active agents and completed blockers before dispatch
risk_tier: C
status: in_progress
depends_on: [CP-07-04]
objective: Provide a deterministic decision boundary that returns all agent and dependency blockers before wake claim/dispatch.
invariants:
  - A paused, terminated, or missing agent cannot dispatch work.
  - Every graph blocker must be explicitly complete before dispatch.
  - Scheduler integration remains a later CP-07 task; this PR owns policy only.
tests_required:
  - cargo test --manifest-path src-tauri/Cargo.toml -p altai-control-plane
```
