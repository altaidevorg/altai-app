# Task Packet: CP-07-06 — Durable Retry, Recovery and Dead Letter State

```yaml
task_id: CP-07-06
title: Persist bounded retry and terminal dead-letter decisions in work.db
risk_tier: C
status: in_progress
depends_on: [CP-07-05]
objective: Record failed attempts idempotently, queue bounded retries, and retain terminal dead-letter records in local SQLite.
invariants:
  - work.db is the only desktop durable store.
  - Retry count is transactionally incremented per Work item.
  - A failure after max_retries becomes terminal dead-letter state.
```
