# Task Packet: CP-08-04 — Trusted Host Attempt Adapter

```yaml
task_id: CP-08-04
title: Translate canonical Attempts into opaque executor input
risk_tier: B
status: in_progress
depends_on: [CP-08-03]
objective: Validate canonical Attempt and immutable RunBinding in the host adapter before constructing the execution service request.
invariants:
  - Only dispatched/running Attempts reach the executor boundary.
  - Attempt, WorkItem, and owner identities must match the immutable RunBinding.
  - Agent-service remains independent of control-plane crates.
  - The adapter does not mutate WorkItem state or scheduler policy.
```
