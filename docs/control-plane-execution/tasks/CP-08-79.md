# CP-08-79 — Accept package 074 + program bookkeeping

- **Package:** 074 — Full Gmail multi-account adapter (acceptance)
- **Status:** in review
- **Depends on:** CP-08-76 (#825), CP-08-77 (#826), CP-08-78 (#827)

## Outcome

Package 074 is accepted; the gate's three sentences are three shipped,
tested things across #825–#827:

- **Account isolation** — `ExternalAccount` identity keyed
  `(integration, account_ref)` (reconnect keeps stored id and creation
  time), object storage keyed with the account dimension (wire- and
  SQLite-compatible migration, version 3), and an account-scoped engine
  where the watermark is the account's alone (#825, #826).
- **Scoped credentials** — the `(plugin, account, name)`-addressed
  store where the wrong scope finds nothing, not the neighbor; the
  desktop fronts it over platform secret storage (#825, #827).
- **Idempotent thread/message sync** — canonical mapping where equal
  mailbox entries hash equal, and the live client + commands closing
  the loop end to end (#826, #827).

## Bookkeeping

- Backlog: 074 `in_progress` → `accepted`, evidence #825, #826, #827
  (3 PRs). Overall 28% → 29%; backbone 60% → 61% (070's adapter
  precedent). Ecosystem unchanged per the 071/072/073 precedent — the
  adapter adopts an external system but ships no plugin-facing
  contract beyond 071's.
- 080 (Paperclip downstream/codebase) → `in_progress`: Stage 7 is
  complete and 080's dependencies (034, 060) are accepted. DEC-001
  (2026-08-13) already made the adoption decision — an
  organization-owned downstream preserving upstream history, license
  and base SHA, with modules selected through narrow adapter
  boundaries — superseding the engineering plan's earlier native-only
  stance (§1 of the parent plan records the superseded decision; the
  DECISIONS ledger and this backlog are the program authority).
- Queue → CP-08-80 (080 PR 1): the downstream charter — repository,
  base SHA, license/history preservation, update policy, and the
  concrete module selection through adapter boundaries. "Study" does
  not count as shipped: the charter names modules and boundaries, and
  code adoption follows against it.
- CURRENT_STATE.md: header + 074 acceptance row.

## Verification

- Docs-only change: CI runs its two non-path-filtered legs (changes,
  frontend static); no code paths touched.
