# Task Packet: CP-07-03 — Postgres Wake/Lease Adapter

```yaml
task_id: CP-07-03
title: Persist coalesced wake requests and exclusive work checkout leases in Postgres
risk_tier: C
parent_module: CP-07
status: in_progress
depends_on: [CP-07-01]
objective: |
  Add the deployed Postgres implementation of WakeRepository. Concurrent wake
  producers must coalesce by work item and checkout must record one exclusive
  live lease without an application-side check-then-insert race.
non_goals:
  - Wake claim, lease expiry, or compare-and-clear lifecycle semantics (CP-07-04)
  - Dispatch eligibility, retry, or transport routes
  - PGlite adapter or migration-runner work
allowed_files:
  - src-tauri/crates/altai-control-plane/src/postgres_wake.rs
  - src-tauri/crates/altai-control-plane/src/lib.rs
  - docs/control-plane-execution/WORK_OS_PROGRAM_BACKLOG.md
  - docs/control-plane-execution/tasks/CP-07-03.md
read_first:
  - docs/control-plane-execution/WORK_OS_PROGRAM_BACKLOG.md Sections 3–5
  - src-tauri/crates/altai-control-plane/src/wake_repository.rs
  - src-tauri/crates/altai-control-plane/src/postgres_work_graph.rs
invariants:
  - One persistent wake row exists per WorkItem.
  - Wake source coalescing occurs in Postgres, not through a read-modify-write race.
  - One WorkItem has at most one persisted checkout lease.
  - No wake/lease transport route is added in this slice.
commands: []
events: [wake_requested]
migrations: [bootstrap Postgres tables are created idempotently]
tests_required:
  - cargo test --manifest-path src-tauri/Cargo.toml -p altai-control-plane
  - cargo check --manifest-path src-tauri/Cargo.toml -p altai-control-plane
acceptance:
  - Postgres adapter exposes WakeRepository
  - Atomic upsert coalesces sources by work item
  - Primary-key conflict admits only one checkout lease
stop_conditions:
  - A live Postgres integration environment is required to prove SQL semantics and unavailable
```
