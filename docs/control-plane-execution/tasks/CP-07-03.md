# Task Packet: CP-07-03 — Local SQLite Wake and Lease Adapter

```yaml
task_id: CP-07-03
title: Persist coalesced wake requests and exclusive checkout leases in work.db
risk_tier: C
status: in_progress
depends_on: [CP-07-01, CP-LS-02]
objective: Replace the in-memory CP-07 repository with a transactional local SQLite adapter in the existing workspace work.db.
invariants:
  - No separate local database or database service.
  - One wake row coalesces sources per Work item.
  - A Work item has at most one active checkout lease.
  - Only the lease-owning attempt can release it.
tests_required:
  - cargo test --manifest-path src-tauri/Cargo.toml -p altai-control-plane
acceptance:
  - Wake and lease state survive reopening work.db.
```
