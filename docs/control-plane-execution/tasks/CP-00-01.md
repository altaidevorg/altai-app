# Task Packet: CP-00-01 — ADR Ownership Amendments

```yaml
task_id: CP-00-01
title: Amend ADR 0001/0002 for control-plane scope; add ADR 0003 for control-plane/execution-plane split
risk_tier: A
parent_module: CP-00
status: accepted
depends_on: [CAL-01, CAL-02, CAL-03]
objective: |
  Amend ADR 0001 to state that durable work outlives any renderer through the
  control-plane daemon. Amend ADR 0002 to state that the protocol carries
  control-plane domains in addition to run control. Create ADR 0003 codifying
  the control-plane/execution-plane ownership boundary. Mark conflicting
  ownership sections in the Agent Operations plan as superseded.
non_goals:
  - Implementing any Rust or TypeScript code
  - Creating architecture import tests (that is CP-00-02)
  - Modifying existing Tauri commands or stores
  - Changing the agent-protocol schema
allowed_files:
  - docs/adr/0001-shared-agent-service.md
  - docs/adr/0002-agent-host-protocol.md
  - docs/adr/0003-control-plane-execution-plane-split.md (new)
  - docs/AGENT_OPERATIONS_IMPLEMENTATION_PLAN.md (mark superseded sections only)
read_first:
  - docs/control-plane-execution/CONTEXT.md (full)
  - docs/PAPERCLIP_STYLE_CONTROL_PLANE_ENGINEERING_PLAN.md Section 1 (decision)
  - docs/PAPERCLIP_STYLE_CONTROL_PLANE_ENGINEERING_PLAN.md Section 3 (architecture rules)
  - docs/PAPERCLIP_STYLE_CONTROL_PLANE_ENGINEERING_PLAN.md Section 6 (topology)
  - docs/PAPERCLIP_STYLE_CONTROL_PLANE_ENGINEERING_PLAN.md CP-00 (full)
  - docs/adr/0001-shared-agent-service.md (full)
  - docs/adr/0002-agent-host-protocol.md (full)
invariants:
  - The parent plan's ownership model is the authority. ADRs are amended to align, not to redefine.
  - ADR 0001's existing shared-service decision is preserved; only the lifecycle/ownership scope is expanded.
  - ADR 0002's existing protocol decision is preserved; only the scope is broadened to include control-plane domains.
  - The Agent Operations plan is not deleted; only conflicting ownership sections are marked superseded.
commands: []
events: []
migrations: []
tests_required:
  - "No code tests — this is a documentation task"
  - "Structural check: all three ADRs have Status, Context, Decision, Consequences sections"
acceptance:
  - "ADR 0001 has an amended section noting durable work outlives renderers through control-plane daemon"
  - "ADR 0002 has an amended section noting protocol carries control-plane domains"
  - "ADR 0003 exists and codifies: (a) one control-plane owner, (b) IsanAgent as execution runtime, (c) ownership boundary table"
  - "AGENT_OPERATIONS_IMPLEMENTATION_PLAN.md has inline superseded markers on conflicting ownership sections"
  - "docs/control-plane-execution/DECISIONS.md is updated to reflect the accepted ADR changes"
stop_conditions:
  - "An ADR amendment would contradict the parent plan's non-negotiable architecture rules (Section 3)"
  - "The Agent Operations plan's ownership sections cannot be cleanly identified"
```

## Current relevant code facts

### ADR 0001 — Lifecycle and ownership (lines 52–59)

Currently states:
> "Work that must outlive VS Code requires a separately designed daemon and is
> out of scope for v1."

This must be amended to state that the control-plane daemon (CP-16) makes
durable work outlive any renderer, and that this is now in scope.

### ADR 0002 — Decision (lines 20–56)

Currently defines protocol methods for run control only:
`initialize, workspace/status, config/get, models/list, agents/list,
sessions/list, sessions/get, sessions/create, run/start, run/steer, run/cancel,
run/replay, clarification/respond, context/compact, checkpoints/list,
checkpoints/restore, shutdown`

This must be amended to note that CP-15 will add control-plane method groups:
`organizations/*, goals/*, projects/*, agents/*, work/*, attempts/*,
routines/*, approvals/*, budgets/*, activity/*, integrations/*, plugins/*`

### Agent Operations plan

`docs/AGENT_OPERATIONS_IMPLEMENTATION_PLAN.md` contains ownership sections
that predate the parent plan. Conflicting sections must be marked as superseded
with a reference to the parent plan.

## Expected diff shape

1. **ADR 0001:** Add an `## Amendment 2026-08-03: Control-plane scope` section
   after `Lifecycle and ownership`.
2. **ADR 0002:** Add an `## Amendment 2026-08-03: Control-plane domains` section
   after `Decision`.
3. **ADR 0003:** New file codifying the control-plane/execution-plane split.
4. **AGENT_OPERATIONS_IMPLEMENTATION_PLAN.md:** Add inline `> **Superseded:**
   see PAPERCLIP_STYLE_CONTROL_PLANE_ENGINEERING_PLAN.md §X` markers on
   conflicting ownership sections.
5. **DECISIONS.md:** Update the superseded table to mark ADR amendments as
   effective.

## Required negative tests

N/A — documentation task. However, the reviewer must verify:
- No ADR amendment contradicts parent plan Section 3 rules.
- No amendment weakens the existing security boundary from ADR 0001.

## Compatibility and deletion effects

- ADR 0001 and 0002 statuses change from "Accepted for VS Code/protocol v1" to
  "Accepted, amended for control-plane scope."
- The Agent Operations plan is not deleted; only marked sections are superseded.

## Output/handoff format

Return the standard handoff report. The reviewer updates `CURRENT_STATE.md` to
reflect the accepted ADR changes.