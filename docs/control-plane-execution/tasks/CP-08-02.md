# Task Packet: CP-08-02 — Durable Attempt Lifecycle

```yaml
task_id: CP-08-02
title: Persist coordinator-authorized Attempt lifecycle
risk_tier: C
status: in_progress
depends_on: [CP-08-01]
objective: Make Attempt identity, authority snapshot, and monotonic lifecycle durable in workspace-local work.db.
invariants:
  - Attempt profile and ownership authority are immutable after creation.
  - Lifecycle is monotonic and terminal states never reopen.
  - Attempt state does not directly mutate WorkItem disposition.
  - No PostgreSQL, second local database, or desktop executor ownership change is introduced.
```
