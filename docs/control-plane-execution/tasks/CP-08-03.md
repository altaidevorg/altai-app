# Task Packet: CP-08-03 — AttemptExecutor Boundary

```yaml
task_id: CP-08-03
title: Define host-neutral AttemptExecutor commands
risk_tier: B
status: in_progress
depends_on: [CP-08-02]
objective: Establish typed start, inspect, steer, cancel, and replay operations scoped to immutable Attempt-to-Run bindings.
invariants:
  - Every control operation carries the immutable binding.
  - The contract contains no PM mutation or scheduler policy.
  - Only trusted, bounded execution input crosses into IsanAgent.
```
