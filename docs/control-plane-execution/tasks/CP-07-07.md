# Task Packet: CP-07-07 — Authenticated Wake and Checkout Transport

```yaml
task_id: CP-07-07
title: Expose local wake and checkout commands through authenticated loopback transport
risk_tier: C
status: in_progress
depends_on: [CP-07-06]
objective: Wire the durable SQLite wake/lease port into the daemon and expose bounded bootstrap-authenticated command routes.
invariants:
  - Loopback only; no remote control-plane service is introduced.
  - Every wake/claim/checkout/release mutation requires bootstrap bearer authorization.
  - This transport does not start a scheduler or bypass dispatch eligibility.
```
