# Task Packet: CP-08-01 — Attempt and RunBinding Contracts

```yaml
task_id: CP-08-01
title: Add immutable typed Attempt-to-executor-run bindings
risk_tier: C
status: in_progress
depends_on: [CP-07-08]
objective: Persist one immutable executor Run binding per control-plane Attempt in workspace work.db and expose an authenticated loopback command.
invariants:
  - An Attempt can bind exactly one Run.
  - Repeating the same binding is idempotent; rebinding conflicts.
  - This does not change desktop executor ownership yet.
```
