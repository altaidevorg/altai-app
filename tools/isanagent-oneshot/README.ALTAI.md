# Temporary IsanAgent oneshot pin

This directory is a checkout of IsanAgent at `8c9eef2` plus the M1 oneshot
host API. ALTAI path-depends on it until the upstream PR merges.

See `docs/cli/isanagent-oneshot-api.md`.
