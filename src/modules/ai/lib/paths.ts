/**
 * Resolve a possibly-relative path against the active terminal cwd.
 * Implementation lives in `@altai/agent-ui` (`resolveWorkspacePath`).
 */
export { resolveWorkspacePath as resolvePath } from "@altai/agent-ui";
